name: Android Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Get tag name
        id: tag
        run: echo "TAG_NAME=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Rename APKs with version
        run: |
          TAG="${{ steps.tag.outputs.TAG_NAME }}"
          cp app/build/outputs/apk/debug/app-debug.apk "app/build/outputs/apk/debug/WifiChanger-${TAG}-debug.apk"
          cp app/build/outputs/apk/release/app-release-unsigned.apk "app/build/outputs/apk/release/WifiChanger-${TAG}-release.apk"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.TAG_NAME }}
          name: "Wifi Changer ${{ steps.tag.outputs.TAG_NAME }}"
          body: |
            ## Wifi Changer ${{ steps.tag.outputs.TAG_NAME }}

            Full APK build (not an update — install directly).

            ### Downloads
            - **WifiChanger-${{ steps.tag.outputs.TAG_NAME }}-debug.apk** — for testing
            - **WifiChanger-${{ steps.tag.outputs.TAG_NAME }}-release.apk** — for production (unsigned)
          files: |
            app/build/outputs/apk/debug/WifiChanger-${{ steps.tag.outputs.TAG_NAME }}-debug.apk
            app/build/outputs/apk/release/WifiChanger-${{ steps.tag.outputs.TAG_NAME }}-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
